import ComingSoonPlaceholder from "../components/common/ComingSoonPlaceholder";

/** NotFoundPage.jsx — catch-all for any route outside the Arquitectura de Información (C5). */
export default function NotFoundPage() {
  return (
    <ComingSoonPlaceholder
      title="Página no encontrada"
      description="La ruta a la que intentaste acceder no existe en Convoka."
    />
  );
}
