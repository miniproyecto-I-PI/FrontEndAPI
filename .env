# Base URL of the Django REST Framework backend (see C4 Backlog, TS-01/TS-02).
# Copy this file to ".env" and adjust per environment (local/staging/prod).
# Read in code via `import.meta.env.VITE_API_URL` — see src/services/api.js.
VITE_API_URL=http://localhost:8000/api
